#+title: ROADMAP
#+subtitle: Open follow-ups for the mttm configuration

Follow-up items for the mttm configuration, tracked until resolved.
Priorities use org-mode's default scale, #A to #C: [#A] major, [#B]
moderate, [#C] minor.

Convention: every non-blocking finding from @reviewer (NON-BLOCKING and
NIT severity) is recorded here so it is eventually tackled. Severity
maps to priority: NON-BLOCKING → [#B], NIT → [#C]. BLOCKING findings are
fixed before the commit and never reach the roadmap. Resolved items are
archived to =CHANGELOG.org= (org-mode archive pattern); this file tracks
only open work.

* Items

** [#B] Orchestrator: =git branch *= also permits =git branch -D=
:PROPERTIES:
:FILE: agent/orchestrator.md
:END:

The =git branch *= allow pattern also covers =git branch -D= (force
delete), broader than the =git branch -d= the feature workflow uses.
Inherent to OpenCode's prefix-based bash matching; revisit if OpenCode
gains argument-aware matching. (@reviewer, NON-BLOCKING, on a1d3f95.)

** [#B] Reviewer: pipe-based bash write bypass
:PROPERTIES:
:FILE: agent/reviewer.md
:END:

OpenCode matches bash patterns on command prefixes, so a pipe such as
=git diff | git apply= can still write despite the =*: deny= catch-all.
Documented as accepted risk in the reviewer prompt; revisit if OpenCode
gains argument- or pipe-aware matching. (@reviewer, NON-BLOCKING.)
